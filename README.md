# 🚀 AI-Translate - Интеллектуальный помощник распознавания и перевода текстов

## 📋 Описание проекта

AI-Translate - это MVP решение для автоматического анализа медиа-файлов (видео, аудио, изображения), извлечения текста из них и перевода на другие языки. Проект демонстрирует использование современных нейросетевых технологий распознавания речи и генерации перевода текстов.

## ✨ Функциональность

### Базовые функции:
- ✅ Приём входных медиа-файлов (.mp4 / .webm / .mp3 / .jpg / .jpeg / .png)
- ✅ Распознавание речи и текста из медиа-файлов
- ✅ Перевод на русский, казахский и английский языки

### Дополнительные функции (бонус):
- 🎯 Определение положения текстов на изображениях
- 🎯 Замена исходного изображения на новое с переведенным текстом
- 🎯 Определение спикера и временных меток в аудио/видео
- 🎯 Мультиязычный перевод (4+ языков)
- 🎯 Генерация аудио-файла с голосом (Text to Speech)
- 🎯 Выбор голоса при генерации аудио

## 🛠 Технологический стек

### Backend:
- **FastAPI** - веб-фреймворк
- **Whisper** (OpenAI) - Speech to Text
- **EasyOCR** / **PaddleOCR** - Image to Text
- **NLLB** (Meta) - Text to Text Translation
- **Coqui TTS** - Text to Speech (бонус)

### Frontend:
- **React** + **TypeScript** - Telegram Mini App
- **Tailwind CSS** - Стилизация

### Инфраструктура:
- **Docker** + **Docker Compose**
- **PM2** - управление процессами
- **Python 3.11+**
- **Node.js 18+**

---

## 🚀 Быстрый старт

### Предварительные требования:
- **Python 3.11+**
- **Node.js 18+** и **npm**
- **FFmpeg** (для обработки видео/аудио)
- **Docker** (для запуска через Docker)
- **PM2** (для деплоя на сервере)

### Вариант 1: Запуск через Docker (рекомендуется для разработки)

1. **Клонируйте репозиторий:**
```bash
git clone <repository-url>
cd AITranslate
```

2. **Запустите проект через Docker Compose:**
```bash
docker-compose up --build
```

**Примечание:** Первый запуск может занять время из-за загрузки AI-моделей (Whisper, EasyOCR, NLLB). Это нормально!

3. **Откройте в браузере:**
- Web версия: http://localhost:3000
- API документация: http://localhost:8000/docs
- Telegram Mini App: настройте через BotFather (см. раздел ниже)

### Вариант 2: Локальная разработка (без Docker)

#### Backend:
```bash
cd backend
python3 -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate

# Установка зависимостей (может занять время)
pip install -r requirements.txt

# Установка FFmpeg (если не установлен)
# Ubuntu/Debian: sudo apt-get install ffmpeg
# macOS: brew install ffmpeg

# Создание директорий
mkdir -p uploads models

# Запуск сервера
uvicorn app.main:app --reload --port 8000
```

#### Frontend:
```bash
cd frontend/telegram-miniapp
npm install
npm run dev
```

### Вариант 3: Деплой на сервере через PM2 (Production)

#### 1. Установка PM2
```bash
npm install -g pm2
```

#### 2. Подготовка проекта
```bash
# Клонирование репозитория
git clone <repository-url>
cd AITranslate

# Установка зависимостей бэкенда
cd backend
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
cd ..

# Установка зависимостей фронтенда
cd frontend/telegram-miniapp
npm install
npm run build
cd ../..

# Создание необходимых директорий
mkdir -p backend/uploads backend/models logs
```

#### 3. Запуск через PM2
```bash
# Запуск всех приложений
pm2 start ecosystem.config.js

# Или по отдельности:
pm2 start ecosystem.config.js --only ai-translate-backend
pm2 start ecosystem.config.js --only ai-translate-frontend
```

#### 4. Управление приложениями
```bash
# Просмотр статуса
pm2 status

# Просмотр логов
pm2 logs

# Перезапуск
pm2 restart all

# Остановка
pm2 stop all

# Автозапуск при перезагрузке сервера
pm2 save
pm2 startup
```

#### 5. Обновление проекта
```bash
pm2 stop all
git pull
cd backend && source venv/bin/activate && pip install -r requirements.txt && cd ..
cd frontend/telegram-miniapp && npm install && npm run build && cd ../..
pm2 restart all
```

---

## 📁 Структура проекта

```
AITranslate/
├── backend/                 # FastAPI backend
│   ├── app/
│   │   ├── api/            # API роуты
│   │   │   └── routes/     # Эндпоинты
│   │   ├── core/           # Конфигурация и модели
│   │   ├── services/       # Сервисы обработки AI
│   │   └── main.py         # Точка входа
│   ├── models/             # Локальные AI модели
│   ├── uploads/            # Временные файлы
│   └── requirements.txt
├── frontend/
│   └── telegram-miniapp/   # Telegram Mini App (React + TS)
│       ├── src/
│       │   ├── components/ # React компоненты
│       │   └── App.tsx     # Главный компонент
│       └── package.json
├── ecosystem.config.js     # Конфигурация PM2
├── docker-compose.yml      # Docker конфигурация
└── README.md
```

---

## 🏗️ Архитектура проекта

### Архитектурная диаграмма

```
┌─────────────────────────────────────────────────────────┐
│                    Frontend Layer                       │
│  ┌──────────────────┐      ┌──────────────────┐       │
│  │ Telegram Mini App│      │   Web Interface  │       │
│  │  (React + TS)    │      │   (React + TS)   │       │
│  └────────┬─────────┘      └────────┬─────────┘       │
└───────────┼──────────────────────────┼─────────────────┘
            │                          │
            └──────────┬───────────────┘
                       │ HTTP/REST API
┌──────────────────────┼──────────────────────────────────┐
│              Backend API Layer (FastAPI)                 │
│  ┌──────────────────────────────────────────────────┐   │
│  │              API Routes                          │   │
│  │  /api/upload  /api/recognize  /api/translate   │   │
│  │  /api/process  /api/tts                        │   │
│  └──────────────────┬───────────────────────────────┘   │
│                     │                                    │
│  ┌──────────────────┼───────────────────────────────┐   │
│  │         Service Layer                            │   │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐      │   │
│  │  │ Whisper  │  │   OCR    │  │Translation│      │   │
│  │  │ Service  │  │ Service  │  │ Service  │      │   │
│  │  └──────────┘  └──────────┘  └──────────┘      │   │
│  │  ┌──────────┐  ┌──────────────────────────┐    │   │
│  │  │   TTS    │  │   Media Processor        │    │   │
│  │  │ Service  │  │   (Orchestrator)         │    │   │
│  │  └──────────┘  └──────────────────────────┘    │   │
│  └──────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────┘
                       │
┌──────────────────────┼──────────────────────────────────┐
│              AI Models Layer                            │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐             │
│  │ Whisper  │  │ EasyOCR  │  │   NLLB   │             │
│  │ (STT)    │  │  (OCR)   │  │(Translate)│            │
│  └──────────┘  └──────────┘  └──────────┘             │
│  ┌──────────┐                                         │
│  │ Coqui TTS│                                         │
│  │  (TTS)   │                                         │
│  └──────────┘                                         │
└─────────────────────────────────────────────────────────┘
```

### Компоненты системы

#### Backend (FastAPI)

**`app/core/`** - Базовая конфигурация
- `config.py` - Настройки приложения, пути, языки
- `models.py` - Pydantic модели для валидации данных

**`app/api/routes/`** - API эндпоинты
- `upload.py` - Загрузка медиа-файлов
- `recognize.py` - Распознавание текста/речи
- `translate.py` - Перевод текста
- `process.py` - Полная обработка (распознавание + перевод)
- `tts.py` - Генерация речи (бонус)

**`app/services/`** - Бизнес-логика и интеграция с AI
- `whisper_service.py` - Speech to Text
- `ocr_service.py` - Optical Character Recognition
- `translation_service.py` - Перевод текстов
- `tts_service.py` - Text to Speech
- `media_processor.py` - Оркестрация всех сервисов

#### Frontend (React + TypeScript)

**Основные компоненты:**
- `App.tsx` - Главный компонент с управлением состоянием
- `FileUpload.tsx` - Загрузка файлов с Drag & Drop
- `LanguageSelector.tsx` - Выбор языков перевода
- `ResultDisplay.tsx` - Отображение результатов
- `ProcessingSteps.tsx` - Индикация процесса обработки

#### AI Модели

**Whisper (Speech to Text)**
- Модель: `base` (баланс скорости/качества)
- Поддержка: 99+ языков
- Временные метки для каждого слова

**EasyOCR (Image to Text)**
- Поддержка: английский, русский, казахский
- Определение bounding boxes текста
- Оценка уверенности распознавания

**NLLB (Translation)**
- Модель: `facebook/nllb-200-distilled-600M`
- Поддержка: 200+ языков
- Высокое качество перевода

**Coqui TTS (Text to Speech)**
- Мультиязычная генерация
- Выбор голоса
- Естественное звучание

### Поток обработки данных

#### Обработка изображения:
```
Изображение → OCR Service → Текст + BBoxes
                ↓
         Translation Service → Переводы на языки
                ↓
    (Опционально) Замена текста на изображении
                ↓
            JSON Response
```

#### Обработка аудио/видео:
```
Аудио/Видео → Извлечение аудио (если видео)
                ↓
         Whisper Service → Текст + Timestamps
                ↓
         Translation Service → Переводы на языки
                ↓
    (Опционально) TTS Service → Аудио файл
                ↓
            JSON Response
```

---

## 📊 API Endpoints

### Основные эндпоинты:

- `POST /api/upload` - Загрузка медиа-файла
- `POST /api/recognize` - Распознавание текста/речи из файла
- `POST /api/translate` - Перевод текста на указанные языки
- `POST /api/process` - Полная обработка: распознавание + перевод + опционально TTS
- `POST /api/tts` - Генерация аудио из текста (бонус)
- `GET /api/health` - Проверка здоровья сервиса
- `GET /docs` - Интерактивная документация API (Swagger)

### Полная документация API доступна по адресу: http://localhost:8000/docs

---

---

## 🌐 Настройка Telegram Mini App

1. Создайте бота через [@BotFather](https://t.me/BotFather)
2. Используйте команду `/newapp` для создания Mini App
3. Укажите URL вашего frontend (например, `https://your-domain.com`)
4. Для локальной разработки используйте [ngrok](https://ngrok.com/):
```bash
ngrok http 3000
```
5. Используйте полученный URL в BotFather

---

## ⚙️ Настройка Nginx (для Production)

```nginx
# /etc/nginx/sites-available/ai-translate
server {
    listen 80;
    server_name your-domain.com;

    # Frontend
    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }

    # Backend API
    location /api {
        proxy_pass http://localhost:8000;
        proxy_http_version 1.1;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Статические файлы (uploads)
    location /uploads {
        proxy_pass http://localhost:8000;
    }
}
```

---

## 🐛 Решение проблем

### Backend не запускается

```bash
# Проверьте логи
pm2 logs ai-translate-backend --lines 50

# Проверьте, что виртуальное окружение активировано
cd backend
source venv/bin/activate
python3 -c "import uvicorn; print('OK')"
```

### Frontend не запускается

```bash
# Проверьте логи
pm2 logs ai-translate-frontend --lines 50

# Убедитесь, что фронтенд собран
cd frontend/telegram-miniapp
npm run build
```

### Модели не загружаются

```bash
# Проверьте права доступа
chmod -R 755 backend/models

# Проверьте место на диске (нужно ~5-10 GB)
df -h
```

### FFmpeg не установлен

```bash
# Ubuntu/Debian
sudo apt-get update
sudo apt-get install ffmpeg

# macOS
brew install ffmpeg
```

---

## ⚠️ Важные замечания

1. **Первая загрузка моделей**: При первом запуске AI-модели будут скачиваться автоматически. Это может занять 10-30 минут.

2. **Место на диске**: Убедитесь, что на сервере достаточно места (минимум 5-10 GB для моделей).

3. **Память**: Backend может потреблять до 2GB RAM при работе с моделями.

4. **Порты**:
   - Backend: `8000`
   - Frontend: `3000`
   - Убедитесь, что порты свободны или измените в конфигурации

5. **GPU**: Ускоряет обработку, но не обязателен (работает на CPU)

---

## 🔮 Будущие улучшения

- [ ] Поддержка пакетной обработки файлов
- [ ] История переводов с сохранением в базе данных
- [ ] Экспорт результатов в различные форматы (SRT, TXT, JSON)
- [ ] Оптимизация моделей для мобильных устройств
- [ ] Поддержка облачного хранения результатов
- [ ] Микросервисная архитектура для каждого AI-сервиса
- [ ] Очереди задач (Celery/RQ) для асинхронной обработки

---

## 📝 Лицензия

Проект создан для хакатона AI-Translate.

## 👥 Авторы

Команда хакатона AI-Translate

## 🙏 Благодарности

- OpenAI за модель Whisper
- Meta за модель NLLB
- EasyOCR за библиотеку распознавания текста
- Coqui за библиотеку TTS
